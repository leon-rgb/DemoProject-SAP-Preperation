server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/miniconcur
    username: postgres
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
management:
  endpoints.web.exposure.include: health,info
  endpoint.health.probes.enabled: true
